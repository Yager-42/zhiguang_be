<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.tongji.promotion.mapper.PaidBoostDeliveryMapper">

    <resultMap id="PaidBoostDeliveryResultMap" type="com.tongji.promotion.model.PaidBoostDelivery">
        <id column="id" property="id"/>
        <result column="campaign_id" property="campaignId"/>
        <result column="channel" property="channel"/>
        <result column="post_id" property="postId"/>
        <result column="viewer_user_id" property="viewerUserId"/>
        <result column="delivery_bucket_start_at" property="deliveryBucketStartAt"/>
        <result column="delivery_count" property="deliveryCount"/>
        <result column="unit_price_snapshot" property="unitPriceSnapshot"/>
        <result column="captured_amount" property="capturedAmount"/>
        <result column="settle_business_ref" property="settleBusinessRef"/>
        <result column="status" property="status"/>
        <result column="settled_at" property="settledAt"/>
        <result column="created_at" property="createdAt"/>
        <result column="updated_at" property="updatedAt"/>
    </resultMap>

    <insert id="upsertPending" parameterType="com.tongji.promotion.model.PaidBoostDelivery">
        INSERT INTO promotion_boost_delivery (
            id, campaign_id, channel, post_id, viewer_user_id, delivery_bucket_start_at, delivery_count,
            unit_price_snapshot, captured_amount, settle_business_ref, status, settled_at, created_at, updated_at
        ) VALUES (
            #{id}, #{campaignId}, #{channel}, #{postId}, #{viewerUserId}, #{deliveryBucketStartAt}, #{deliveryCount},
            #{unitPriceSnapshot}, #{capturedAmount}, #{settleBusinessRef}, #{status}, #{settledAt}, #{createdAt}, #{updatedAt}
        )
        ON DUPLICATE KEY UPDATE
            delivery_count = delivery_count + VALUES(delivery_count),
            updated_at = VALUES(updated_at)
    </insert>

    <select id="listPendingBefore" resultMap="PaidBoostDeliveryResultMap">
        SELECT *
        FROM promotion_boost_delivery
        WHERE status = 'PENDING'
          AND delivery_bucket_start_at &lt; #{now}
        ORDER BY delivery_bucket_start_at ASC, id ASC
        LIMIT #{limit}
    </select>

    <update id="markSettledWithAmount">
        UPDATE promotion_boost_delivery
        SET captured_amount = #{capturedAmount},
            status = 'SETTLED',
            settled_at = #{settledAt},
            updated_at = #{settledAt}
        WHERE id = #{id}
    </update>

    <select id="listByCampaignId" resultMap="PaidBoostDeliveryResultMap">
        SELECT *
        FROM promotion_boost_delivery
        WHERE campaign_id = #{campaignId}
        ORDER BY delivery_bucket_start_at DESC, id DESC
        LIMIT #{limit}
        OFFSET #{offset}
    </select>
</mapper>
