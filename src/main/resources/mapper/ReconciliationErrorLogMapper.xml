<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.tongji.reconciliation.mapper.ReconciliationErrorLogMapper">

    <insert id="insert" parameterType="com.tongji.reconciliation.model.ReconciliationErrorLog">
        INSERT INTO reconciliation_error_log (
            id, task_id, execution_duration_ms, error_message, stack_trace, created_at
        ) VALUES (
            #{id}, #{taskId}, #{executionDurationMs}, #{errorMessage}, #{stackTrace}, #{createdAt}
        )
    </insert>
</mapper>
