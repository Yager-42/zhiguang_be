<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.tongji.wallet.mapper.WalletEscrowMapper">

    <resultMap id="WalletEscrowResultMap" type="com.tongji.wallet.model.WalletEscrow">
        <id column="id" property="id"/>
        <result column="business_type" property="businessType"/>
        <result column="business_ref" property="businessRef"/>
        <result column="payer_user_id" property="payerUserId"/>
        <result column="payee_user_id" property="payeeUserId"/>
        <result column="amount" property="amount"/>
        <result column="status" property="status"/>
        <result column="expires_at" property="expiresAt"/>
        <result column="created_at" property="createdAt"/>
        <result column="updated_at" property="updatedAt"/>
    </resultMap>

    <insert id="insert" parameterType="com.tongji.wallet.model.WalletEscrow">
        INSERT INTO wallet_escrow (
            id, business_type, business_ref, payer_user_id, payee_user_id, amount, status, expires_at, created_at, updated_at
        ) VALUES (
            #{id}, #{businessType}, #{businessRef}, #{payerUserId}, #{payeeUserId}, #{amount}, #{status}, #{expiresAt}, #{createdAt}, #{updatedAt}
        )
    </insert>

    <select id="findById" parameterType="long" resultMap="WalletEscrowResultMap">
        SELECT *
        FROM wallet_escrow
        WHERE id = #{id}
        LIMIT 1
    </select>

    <select id="findByIdForUpdate" parameterType="long" resultMap="WalletEscrowResultMap">
        SELECT *
        FROM wallet_escrow
        WHERE id = #{id}
        LIMIT 1
        FOR UPDATE
    </select>

    <select id="findByBusinessRef" parameterType="string" resultMap="WalletEscrowResultMap">
        SELECT *
        FROM wallet_escrow
        WHERE business_ref = #{businessRef}
        LIMIT 1
    </select>

    <update id="transitionStatus">
        UPDATE wallet_escrow
        SET status     = #{toStatus},
            updated_at = NOW(3)
        WHERE id = #{id}
          AND status = #{fromStatus}
    </update>
</mapper>
