<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.tongji.comment.mapper.PendingCommentMapper">

    <resultMap id="PendingCommentResultMap" type="com.tongji.comment.model.PendingComment">
        <id property="pendingCommentId" column="pending_comment_id" />
        <result property="postId" column="post_id" />
        <result property="creatorId" column="creator_id" />
        <result property="clientRequestId" column="client_request_id" />
        <result property="status" column="status" />
        <result property="createTime" column="create_time" />
        <result property="updateTime" column="update_time" />
    </resultMap>

    <insert id="insert" parameterType="com.tongji.comment.model.PendingComment">
        INSERT INTO pending_comments (
            pending_comment_id, post_id, creator_id, client_request_id, status, create_time, update_time
        ) VALUES (
            #{pendingCommentId}, #{postId}, #{creatorId}, #{clientRequestId}, #{status}, #{createTime}, #{updateTime}
        )
    </insert>

    <select id="findById" resultMap="PendingCommentResultMap">
        SELECT *
        FROM pending_comments
        WHERE pending_comment_id = #{pendingCommentId}
        LIMIT 1
    </select>

    <select id="findByCreatorAndClientRequestId" resultMap="PendingCommentResultMap">
        SELECT *
        FROM pending_comments
        WHERE creator_id = #{creatorId}
          AND client_request_id = #{clientRequestId}
        LIMIT 1
    </select>

    <update id="updateStatus">
        UPDATE pending_comments
        SET status = #{status},
            update_time = NOW(3)
        WHERE pending_comment_id = #{pendingCommentId}
    </update>

    <update id="updateStatusIfCurrent">
        UPDATE pending_comments
        SET status = #{status},
            update_time = NOW(3)
        WHERE pending_comment_id = #{pendingCommentId}
          AND status = #{currentStatus}
    </update>

    <update id="updateStatusByCreatorAndClientRequestId">
        UPDATE pending_comments
        SET status = #{status},
            update_time = NOW(3)
        WHERE creator_id = #{creatorId}
          AND client_request_id = #{clientRequestId}
    </update>
</mapper>
