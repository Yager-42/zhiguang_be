<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.tongji.promotion.bprime.mapper.PromotionAuctionDecisionMapper">

    <resultMap id="PromotionAuctionDecisionRecordResultMap" type="com.tongji.promotion.bprime.model.PromotionAuctionDecisionRecord">
        <id column="id" property="id"/>
        <result column="decision_id" property="decisionId"/>
        <result column="command_id" property="commandId"/>
        <result column="auction_window_id" property="auctionWindowId"/>
        <result column="decision_type" property="decisionType"/>
        <result column="accepted" property="accepted"/>
        <result column="rejection_reason" property="rejectionReason"/>
        <result column="payload_json" property="payloadJson"/>
        <result column="created_at" property="createdAt"/>
    </resultMap>

    <insert id="insertIgnore" parameterType="com.tongji.promotion.bprime.model.PromotionAuctionDecisionRecord">
        INSERT IGNORE INTO promotion_auction_decision (
            id, decision_id, command_id, auction_window_id, decision_type, accepted, rejection_reason, payload_json, created_at
        ) VALUES (
            #{id}, #{decisionId}, #{commandId}, #{auctionWindowId}, #{decisionType}, #{accepted}, #{rejectionReason}, CAST(#{payloadJson} AS JSON), #{createdAt}
        )
    </insert>

    <select id="findByDecisionId" resultMap="PromotionAuctionDecisionRecordResultMap">
        SELECT * FROM promotion_auction_decision WHERE decision_id = #{decisionId} LIMIT 1
    </select>

    <select id="listAcceptedByWindow" resultMap="PromotionAuctionDecisionRecordResultMap">
        SELECT *
        FROM promotion_auction_decision
        WHERE auction_window_id = #{auctionWindowId}
          AND accepted = 1
        ORDER BY created_at ASC, id ASC
    </select>
</mapper>
