<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.tongji.wallet.mapper.WalletLedgerMapper">

    <resultMap id="WalletLedgerEntryResultMap" type="com.tongji.wallet.model.WalletLedgerEntry">
        <id column="id" property="id"/>
        <result column="owner_user_id" property="ownerUserId"/>
        <result column="counterparty_user_id" property="counterpartyUserId"/>
        <result column="escrow_id" property="escrowId"/>
        <result column="business_type" property="businessType"/>
        <result column="business_ref" property="businessRef"/>
        <result column="direction" property="direction"/>
        <result column="reason" property="reason"/>
        <result column="amount" property="amount"/>
        <result column="available_delta" property="availableDelta"/>
        <result column="held_delta" property="heldDelta"/>
        <result column="escrowed_delta" property="escrowedDelta"/>
        <result column="balance_available_after" property="balanceAvailableAfter"/>
        <result column="balance_held_after" property="balanceHeldAfter"/>
        <result column="balance_escrowed_after" property="balanceEscrowedAfter"/>
        <result column="created_at" property="createdAt"/>
    </resultMap>

    <insert id="insert" parameterType="com.tongji.wallet.model.WalletLedgerEntry">
        INSERT INTO wallet_ledger (
            id, owner_user_id, counterparty_user_id, escrow_id, business_type, business_ref,
            direction, reason, amount, available_delta, held_delta, escrowed_delta,
            balance_available_after, balance_held_after, balance_escrowed_after, created_at
        ) VALUES (
            #{id}, #{ownerUserId}, #{counterpartyUserId}, #{escrowId}, #{businessType}, #{businessRef},
            #{direction}, #{reason}, #{amount}, #{availableDelta}, #{heldDelta}, #{escrowedDelta},
            #{balanceAvailableAfter}, #{balanceHeldAfter}, #{balanceEscrowedAfter}, #{createdAt}
        )
    </insert>

    <select id="findByBusinessRef" parameterType="string" resultMap="WalletLedgerEntryResultMap">
        SELECT *
        FROM wallet_ledger
        WHERE business_ref = #{businessRef}
        LIMIT 1
    </select>

    <select id="listByOwnerUserId" resultMap="WalletLedgerEntryResultMap">
        SELECT *
        FROM wallet_ledger
        WHERE owner_user_id = #{ownerUserId}
        ORDER BY created_at DESC, id DESC
        LIMIT #{limit} OFFSET #{offset}
    </select>
</mapper>
