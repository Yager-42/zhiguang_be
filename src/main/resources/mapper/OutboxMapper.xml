<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.tongji.outbox.OutboxMapper">
    <insert id="insert">
        INSERT INTO outbox (id, aggregate_type, aggregate_id, type, payload, created_at)
        VALUES (#{id}, #{aggregateType}, #{aggregateId}, #{type}, #{payload}, NOW(3))
    </insert>

    <insert id="insertUnique">
        INSERT INTO outbox (id, event_key, aggregate_type, aggregate_id, type, payload, created_at)
        VALUES (#{id}, #{eventKey}, #{aggregateType}, #{aggregateId}, #{type}, #{payload}, NOW(3))
    </insert>

    <delete id="deleteCreatedBefore">
        DELETE FROM outbox
        WHERE created_at &lt; #{cutoff}
        ORDER BY created_at, id
        LIMIT #{limit}
    </delete>
</mapper>

